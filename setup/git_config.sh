#!/bin/bash
sudo apt-get install -y git-core git
git config --global push.default simple

# Alter the next two lines as noted, and un-comment
#git config --global user.name '<replace this with your user's name>'
#git config --global user.email '<replace this with your user's email>'

#e.g. 
#git config --global user.name 'Bob Douglas'
#git config --global user.email 'myuser@ussinnovate.com'
